{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Transit App",
        "description": "Enter your Transit App API key and the location to search for nearby stops.",
        "data": {
          "api_key": "API key",
          "latitude": "Latitude",
          "longitude": "Longitude",
          "radius": "Search radius (metres)",
          "search_filter": "Filter (optional)"
        },
        "data_description": {
          "search_filter": "Only show stops whose name, code, or a route number/name contains this text - handy when a large radius turns up a lot of stops."
        }
      },
      "stops": {
        "title": "Choose stops to track",
        "description": "Select the stops you want next-departure sensors for. The same physical stop can appear more than once if it's served by more than one transit agency feed - each [bracketed] tag is a different feed, both are valid to pick.",
        "data": {
          "stops": "Stops"
        }
      },
      "filters": {
        "title": "Save API calls (optional)",
        "description": "These are optional and all off by default. Use them to skip polling Transit App when nobody needs the data, to help stay within your API key's call quota.",
        "data": {
          "presence_entities": "Only poll while one of these is home",
          "quiet_hours_start": "Quiet hours start",
          "quiet_hours_end": "Quiet hours end",
          "quiet_days": "Quiet days (no polling at all)"
        }
      }
    },
    "error": {
      "invalid_auth": "The API key was rejected by Transit App.",
      "cannot_connect": "Could not reach the Transit App API.",
      "no_stops_found": "No stops were found near that location. Try a larger radius.",
      "no_stops_match_filter": "No stops nearby matched that filter. Try broadening or clearing it.",
      "no_stops_selected": "Select at least one stop."
    },
    "abort": {
      "already_configured": "This combination of API key and stops is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Transit App options",
        "menu_options": {
          "search_stops": "Add or change tracked stops",
          "filters": "Save API calls (presence / quiet hours)"
        }
      },
      "search_stops": {
        "title": "Search for stops",
        "description": "Search again to add stops. Stops you already track stay selected even if this search doesn't find them again.",
        "data": {
          "latitude": "Latitude",
          "longitude": "Longitude",
          "radius": "Search radius (metres)",
          "search_filter": "Filter (optional)"
        },
        "data_description": {
          "search_filter": "Only show stops whose name, code, or a route number/name contains this text - handy when a large radius turns up a lot of stops."
        }
      },
      "stops": {
        "title": "Choose stops to track",
        "description": "Select the stops you want next-departure sensors for. The same physical stop can appear more than once if it's served by more than one transit agency feed - each [bracketed] tag is a different feed, both are valid to pick.",
        "data": {
          "stops": "Stops"
        }
      },
      "filters": {
        "title": "Save API calls (optional)",
        "description": "These are optional and all off by default. Use them to skip polling Transit App when nobody needs the data, to help stay within your API key's call quota.",
        "data": {
          "presence_entities": "Only poll while one of these is home",
          "quiet_hours_start": "Quiet hours start",
          "quiet_hours_end": "Quiet hours end",
          "quiet_days": "Quiet days (no polling at all)"
        }
      }
    },
    "error": {
      "invalid_auth": "The API key was rejected by Transit App.",
      "cannot_connect": "Could not reach the Transit App API.",
      "no_stops_found": "No stops were found near that location. Try a larger radius.",
      "no_stops_match_filter": "No stops nearby matched that filter. Try broadening or clearing it.",
      "no_stops_selected": "Select at least one stop."
    }
  },
  "services": {
    "refresh": {
      "name": "Refresh departures now",
      "description": "Immediately polls Transit App for the freshest departures, bypassing any configured quiet hours/days and presence filter. Useful for on-demand debugging without waiting for the next scheduled poll or changing your quota-saving settings.",
      "fields": {
        "config_entry_id": {
          "name": "Transit App integration",
          "description": "Which Transit App config entry to refresh. Refreshes all of them if omitted."
        }
      }
    }
  }
}
